# Telegram Kids — план разработки полной версии

## Статус сборки
- [x] JDK 17, Android SDK, NDK r26, CMake — установлены
- [x] TDLib собран для arm64-v8a, armeabi-v7a, x86_64, x86
- [x] React Native проект создан, TDLib подключён (TdLibModule.kt, TdLibPackage.kt, libtdjni.so)
- [x] Фундамент (feature flags, remote theming, i18n) — реализован, верифицирован
- [ ] Первый запуск на устройстве (TelegramKidsPhone, Windows)

---

## Фаза 1 — Фундамент (TDLib + авторизация + чаты)
*Цель: ребёнок может войти и написать сообщение*

- [ ] `setup-rn.sh` — создать React Native проект, подключить TDLib
- [ ] `TdLib.js` — singleton-клиент, инициализация, обработка событий
- [ ] `LoginScreen` — авторизация через TDLib (телефон → код → 2FA если есть)
- [ ] `ChatsScreen` — список диалогов через TDLib (аватары, превью, бейджи)
- [ ] `MessagesScreen` — переписка (текст, время, статус прочтения)
- [ ] Отправка текстовых сообщений
- [ ] Базовая навигация (таб-бар: Чаты, Контакты, Настройки)
- [ ] Родительский контроль — WebSocket к нашему серверу, блокировка чатов

## Фаза 2 — Медиафайлы
*Цель: всё кроме стикеров и кружочков*

| Задача | Что нужно | Срок |
|---|---|---|
| Фото — отправка + просмотр | gallery picker, fullscreen zoom, свайп | 2–3 нед |
| **Видео — отправка** | **сжатие, прогресс загрузки, плеер с seek-баром** | **3–4 нед** |
| Голосовые сообщения | запись удержанием, волновая форма, плеер | 2–3 нед |
| Файлы / документы | file picker, иконки типов, прогресс скачивания | 1–2 нед |
| Превью ссылок | og:title, og:image парсинг | 1 нед |
| Reply / Forward | ответ на сообщение, пересылка | 1–2 нед |

## Фаза 3 — Богатый контент
*Цель: полноценный набор сообщений*

| Задача | Что нужно | Срок |
|---|---|---|
| Эмодзи-панель | стандартные + Telegram Emoji, недавние | 1–2 нед |
| **Стикеры** | **браузер паков, Lottie-анимация, поиск** | **5–6 нед** |
| **GIF** | **поиск через Tenor/Giphy, рендеринг** | **2–3 нед** |
| **Видео-кружочки** | **круговая маска камеры, запись, круглый плеер** | **4–5 нед** |
| **Реакции на сообщения** | **панель эмодзи-реакций, анимации появления** | **2–3 нед** |
| Форматирование текста | жирный, курсив, моноширинный, ссылки | 1 нед |

## Фаза 4 — Истории (Stories)
*Цель: просмотр и создание историй*

| Задача | Что нужно | Срок |
|---|---|---|
| **Полный цикл Stories** | **просмотрщик со свайпом, создание с текстом/стикерами, камера** | **7–9 нед** |
| Бар историй | верхняя часть ChatsScreen, аватары с кольцом | 1 нед (входит выше) |
| Реакции и ответы на истории | эмодзи-реакции, reply в личку | 1 нед (входит выше) |

## Фаза 2б — Звонки (голос + видео) [MVP]
*TDLib берёт на себя WebRTC/VoIP — нам нужен только UI и обработка событий*

| Задача | Что нужно | Срок |
|---|---|---|
| Голосовые звонки | `createCall({is_video:false})`, `updateCall`, CallScreen | 2–3 нед |
| Видеозвонки | `createCall({is_video:true})`, переключение камеры, pip-режим | 2–3 нед |
| Входящий звонок | экран входящего звонка, ringtone, отклонение | 1 нед |
| CallScreen.js | кнопки mute/speaker/camera/end, таймер длительности | 1 нед |
| Разрешения | микрофон + камера (Android PermissionsAndroid) | 3–5 дн |
| Родительский контроль — звонки | разрешить/запретить звонки в feature flag | 2–3 дн |

> **Почему это MVP:** безопасные звонки — ключевая функция для родителей и детей.
> TDLib реализует WebRTC/DTLS/SRTP внутри — нам не нужен отдельный WebRTC-стек.
> Оценка с TDLib: **~5–7 нед** (vs. 8–12 нед с нуля).

**Что нужно добавить в feature_flags:**
```sql
calls_voice    -- Голосовые звонки (true = включены)
calls_video    -- Видеозвонки (true = включены)
```

## Фаза 5 — Дополнительный функционал

| Задача | Что нужно | Срок |
|---|---|---|
| Архив чатов | свайп → архивировать, отдельная секция | 1–2 нед |
| Поиск | по сообщениям внутри чата + глобальный | 1–2 нед |
| Профиль | аватар, имя, username, биография, медиагалерея | 1–2 нед |
| **Групповые чаты** | **список участников, роли, экраны управления** | **3–4 нед** |

## ✅ Фаза 1а — Фундамент (ВЫПОЛНЕНО 2026-05-22)
*feature flags + remote theming + i18n — всё до первого экрана*

- [x] Таблица `feature_flags` в `db.py` (name, is_enabled, description)
      Засеяны: stories, video_notes, reactions, calls, stickers_animated,
      voice_messages, file_sharing, search_global
- [x] `GET /feature-flags` в `api_server.py` — публичный, без авторизации
- [x] `src/hooks/useFeatureFlag.js` + `src/store/featureFlags.js` — Zustand + MMKV
- [x] `src/theme/ThemeProvider.js` + `src/theme/useTheme.js` — remote theming
- [x] Таблица `app_themes` — все колонки (цвета/шрифты/радиусы), дефолтная тема засеяна
- [x] `GET /theme` — активная тема, публичный
- [x] Таблица `translations` (lang, namespace, key, value) — RU+EN засеяны
- [x] `GET /translations/{lang}/{namespace}` — публичный
- [x] `src/i18n/i18n.js` — MMKV кэш → сервер в фоне → встроенные locales
- [x] `src/i18n/locales/ru|en/` — common, auth, chats, settings
- [x] `src/i18n/useTranslation.js` — единая точка входа
- [x] `App.js` обновлён — ThemeProvider, initI18n, colors.* вместо hex
- [ ] Добавить `language` в таблицу `parents` + переводы в боте (namespace `bot`)
- [ ] Добавить флаги `calls_voice`, `calls_video` (для Фазы 2б)

## Фаза 6 — Родительский контроль (интеграция с бэкендом)
*Большинство уже реализовано в backend/main.py*

- [ ] WebSocket-клиент в мобильном приложении (заменяет REST polling)
- [ ] Перехват вступления в чат → запрос к серверу → ожидание → блокировка/разрешение
- [ ] Перехват начала переписки с ботом → аналогично
- [ ] Экран «Ожидание разрешения родителя» (PendingScreen — уже есть, подключить к TDLib)
- [ ] Push-уведомления (FCM для Android, APNs для iOS)
- [ ] Логирование активности на сервер

## Фаза 7 — Полировка и публикация
- [ ] Splash screen, иконка приложения
- [ ] Тёмная/светлая тема
- [ ] Локализация (RU + EN)
- [ ] Тестирование на Android (минимум Android 8+)
- [ ] Подписание APK, публикация в Google Play
- [ ] iOS версия (требует Mac для сборки TDLib под iOS)

---

## Технический стек мобильного приложения

```
React Native 0.74
TDLib (libtdjni.so) — через нативный модуль (Kotlin/Java)
react-native-vision-camera — камера
react-native-video — видео плеер
react-native-audio-recorder-player — аудио
react-native-document-picker — файлы
lottie-react-native — анимированные стикеры
react-native-fast-image — кэш изображений
react-native-gesture-handler + reanimated — жесты и анимации
```

## Архитектура мобильного приложения

```
App.js
├── TdLib.js (singleton — связь с TDLib, обработка всех событий)
├── store/ (Zustand — глобальное состояние)
│   ├── chats.js
│   ├── messages.js
│   └── auth.js
├── screens/
│   ├── LoginScreen.js
│   ├── ChatsScreen.js
│   ├── MessagesScreen.js
│   ├── ContactsScreen.js
│   ├── ProfileScreen.js
│   ├── SettingsScreen.js
│   ├── StoriesScreen.js
│   └── PendingScreen.js
├── components/
│   ├── MessageBubble.js
│   ├── MediaViewer.js
│   ├── StickerPicker.js
│   ├── EmojiPicker.js
│   ├── VoiceRecorder.js
│   ├── VideoNote.js
│   └── StoryViewer.js
└── android/
    ├── tdlib/ (libtdjni.so для всех ABI)
    └── app/src/main/java/com/telegramkids/
        ├── TdLibModule.kt (нативный модуль)
        └── TdLibPackage.kt
```

---

## Бэкенд (реализован)
- [x] Бот родителя с полным флоу регистрации
- [x] DOI подтверждение email
- [x] Rate limiting
- [x] БД: parents, children, linking_codes, privacy_policy
- [x] Event-based ожидание решения родителя
- [ ] WebSocket вместо REST для событий управления
- [ ] Push-уведомления родителю (FCM)
- [ ] Перейти с SQLite на PostgreSQL (при росте > 1000 пользователей)
- [ ] Вариант Б оплаты (99 руб/мес) — после MVP

---

---

## Сводная таблица сроков

| Фаза | Содержание | Срок | MVP |
|---|---|---|---|
| 1а | feature flags + remote theming + i18n | ✅ готово | ✅ |
| 1 | TDLib + авторизация + базовые чаты + текст | 2–3 нед | ✅ |
| 2 | Фото, **видео (3–4 нед)**, аудио, файлы, reply | 2–3 мес | ✅ |
| **2б** | **Голосовые + видеозвонки (TDLib WebRTC)** | **5–7 нед** | **✅ MVP** |
| 3 | Стикеры **(5–6 нед)**, GIF **(2–3 нед)**, кружочки **(4–5 нед)**, реакции **(2–3 нед)**, эмодзи | 3–4 мес | — |
| 4 | Stories **(7–9 нед)** | 2 мес | — |
| 5 | Архив, поиск, профили, группы **(3–4 нед)** | 2–3 мес | — |
| 6 | Родительский контроль (WebSocket, push) | 2–3 нед | ✅ |
| 7 | Полировка, публикация | 2–3 нед | ✅ |
| **MVP итого** | **Фазы 1а + 1 + 2 + 2б + 6 + 7** | **~8–10 мес** | |
| **Полная версия** | | **~14–18 мес** | |

> Звонки включены в MVP. TDLib реализует VoIP внутри — нам нужен только UI (~5–7 нед вместо 8–12).

*Обновлено: 2026-05-22*
*Текущий этап: Фаза 1 — первый запуск на Windows-эмуляторе, затем TdLib.js + store/auth.js*
